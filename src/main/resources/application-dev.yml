logging:
  level:
    root: INFO
    com.stocks: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG

spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true